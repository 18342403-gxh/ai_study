{
  "enabled": true,
  "name": "编码规约实时检查",
  "description": "在 AI 写入文件之前，强制检查代码是否符合编码规约：布尔命名（is/has 前缀）、禁止 any、函数不超过 50 行、导入分组正确、中文注释完整、错误处理到位",
  "version": "1",
  "when": {
    "type": "preToolUse",
    "toolTypes": [
      "write"
    ]
  },
  "then": {
    "type": "askAgent",
    "prompt": "在写入文件之前，检查即将写入的代码是否符合以下规约。如果违反任何一条，必须修正后再写入：\n\n1. 布尔变量必须用 is/has/should 前缀（如 isLoading，不能是 loading）\n2. 事件处理函数必须用 handle 前缀（如 handleSubmit）\n3. 禁止使用 any 类型\n4. 单个函数不超过 50 行（组件 render 函数可放宽到 120 行）\n5. 导入顺序：第三方库 → 内部公共模块（components/services/data）→ 相对路径模块\n6. 所有网络请求必须有 try-catch 错误处理\n7. 错误信息必须是中文友好提示\n8. 禁止 console.log（除非是 console.warn/error）\n9. 组件顶部必须有功能说明注释\n10. 复杂逻辑必须有中文行内注释\n11. 使用可选链 ?. 进行空值安全访问\n12. 禁止硬编码 API Key 或 localhost\n\n如果代码完全合规，回复「符合规约」。如果有违反项，列出具体问题和修正建议。"
  }
}